server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/authdb?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: Scorpio123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # create-drop for tests, update for dev, validate for prod
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

graphql:
  sdl:
    location: classpath*:schema/**/*.graphqls

jwt:
  secret: my-very-secret-key
